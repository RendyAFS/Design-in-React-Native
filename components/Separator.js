import { View } from "react-native";

// Membuat Functional Component Separator
const Separator = (props) => {
    return <View style={{ height: props.height }}></View>;
  };
  
  export default Separator;